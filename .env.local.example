# Personal CFO - Example Environment Variables
# Duplicate this file as `.env` (local) and fill in your secrets. This file is safe to commit.

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=public-anon-key
# Server-side key (NEVER expose to browser)
SUPABASE_SERVICE_ROLE_KEY=service-role-key

# --- App ---
# Public URL of the app (use localhost in dev)
APP_URL=http://localhost:3000
NODE_ENV=development

# --- Storage / Uploads ---
# Maximum PDF upload size in MB
MAX_UPLOAD_MB=25

# --- OpenAI (statement parsing) ---
OPENAI_API_KEY=sk-...

# --- Queue (Celery/Redis) ---
# If not using Celery/Redis, you can leave these as-is or blank
REDIS_URL=redis://localhost:6379
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

# --- i18n ---
DEFAULT_LOCALE=en

# --- Currency & Exchange Rates ---
# Primary (requires key):
#   https://v6.exchangerate-api.com/v6/${NEXT_PUBLIC_EXCHANGERATE_API_KEY}/latest/PEN
# Fallback (no key):
#   https://api.exchangerate.fun/latest?base=PEN
# Public key for primary provider (safe for browser use)
NEXT_PUBLIC_EXCHANGERATE_API_KEY=
# (Optional legacy overrides)
EXCHANGE_RATES_API_URL=
EXCHANGE_RATES_API_KEY=

# --- Monitoring (optional) ---
# Sentry DSN for error tracking (optional)
SENTRY_DSN=

# --- Stripe (future; pending approval) ---
# STRIPE_SECRET_KEY=sk_live_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
